let writeOutput = function(text)
{
    document.write(<h1>${text}</h1>);
}

writeOutput("Done");

Let dog = {
name: "Snoopy",
numLegs: 4,
sayNumLegs: function() {
    return " This dog has " + dog.numLegs + " Legs"
}

}